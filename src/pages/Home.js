function Home(props) {
  return (
    <div>
      <h1>Home</h1>
     {/* <ul>
        {props.data && props.data.length > 0 && props.data.map((item) => <li key={item.id}>{item.title}</li>)}
      </ul>*/}
    </div>
  )
}

export default Home;